server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Eliezer134/primer_parcial_esp_BackEnd
          default-label: main
          force-pull: true
          search-paths: configuration

